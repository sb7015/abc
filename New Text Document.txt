hits = rag.search(query, k=rerank_k)   # 12 pages
final_hits = hits[:top_k]     
Cohere's rerank models we want to use also need a output that for each query what was first 12 randk pages and after rerank what is new pages also scores so that somebody can understand the difference it can be exported in same excel file
think on it and give sugestion node for now
from langchain_community.cross_encoders import HuggingFaceCrossEncoder
from langchain.retrievers.document_compressors import CrossEncoderReranker

model = HuggingFaceCrossEncoder(model_name="BAAI/bge-reranker-large")
reranker = CrossEncoderReranker(model=model, top_n=3)
